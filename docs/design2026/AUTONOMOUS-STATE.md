# Состояние — перенос сделан, идёт финальная сверка

**Все шесть участков закончены**: фундамент, настройки, главная с навигацией, начальный экран с
потоками прогрузки, подэкраны, аккаунт. Плюс точечный участок (обвязка главной убрана с начального
экрана, три токена перестали быть литералами, мёртвый `shield_assemble.xml` удалён).

**Полная сборка всех волн: `BUILD SUCCESSFUL`.** Прогон с `--rerun-tasks`, `compileFdroidDebugKotlin`
реально отработал, **прогон не запасной** (ноль падений демона — проверено, потому что именно это
дважды скрывало правду). **6 предупреждений против базы в 12, новых ноль**; единственная лишняя
строка — уведомление Gradle об устаревшем плагине, а не предупреждение компилятора.

**Финальная сверка пройдена.** Все 32 пункта Части 2 подтверждены в коде. Ни одной висящей ссылки
`R.id.`/`binding.` против 89 разметок. Ни одного `#hex` в цветовых атрибутах разметок; все 33 цветовых
атрибута заданы в `AppThemeBase` и переопределены в `ThemeOverlay.Mono`. Тайминги §3 сверены
покадрово и совпали до числа.

Ловушка тем оказалась глубже, чем казалось: корень был в `Widget.Departament.Tile`, то есть **22
плитки в девяти разметках**, а три волны залатали свою плитку по отдельности и оставили причину.
Плюс заливка выбранной строки сервера (12 % синего на чёрно-белом экране) и **разрушающий текст,
у которого вообще не было атрибута** — «Выйти из аккаунта» и «Удалить» оставались красными в моно
вопреки §5.7, а `mono_destructiveText` лежал в `colors.xml` без единого читателя.

Открытым осознанно оставлено одно: **оборот дуги 1100 мс** на кнопке подключения. `CircularProgressIndicator`
держит темп внутри себя; маршрут замены (статичная дуга плюс один `ObjectAnimator` на ROTATION, как
уже сделано в `FlowOverlay`) записан в комментарии над `connect_sweep`, но требует проверки на
устройстве.

Ниже — состояние на момент паузы, оставлено как история хода работы.

---

# Состояние на паузу — 2026-08-04, 21:00 UTC

Владелец отлучился и поручил довести перенос редизайна самостоятельно. Этот файл — точка, с которой
продолжать. Написан в расчёте на то, что читать его будет сессия в **чистом контейнере**: облачная
машина утилизируется после простоя, переписка восстанавливается, файловая система нет. Всё, что не в
git, к тому моменту исчезнет.

## Что сделано

**Дизайн-пакет в репозитории** — `design_handoff_departament_redesign/`. Спецификация на 13 разделов,
`code_reference.md` с картой «значение → место в исходнике прототипа», сам прототип. Открывается:
`КАК-ОТКРЫТЬ-ПРОТОТИП.md`, короткий путь — файл `Прототип departament (один файл).html`, всё вшито,
интернет не нужен.

**Волна фундамента — закончена и проверена.** Токены §8, единый отклик на нажатие §1, компонент
окошка выбора `SelectPopup`. Ripple выключен по всему продукту одной строкой. Имена и API —
`docs/design2026/TOKENS.md`, читать обязательно, иначе заведёшь второй набор имён для того же самого.

**Разведка — закончена.** `docs/design2026/PORT-DELTA.md`, 677 строк: построчная дельта по каждому
экрану с числами и файлами, 32 пункта «что обязано пережить перенос», около 90 недостающих строк,
девять конфликтов для владельца. Плюс 50 скриншотов прототипа в `port-shots/`.

## Что брошено на середине — коммит `bc89d46`

Пять волн остановлены на ходу, чтобы сохранить их работу перед уходом контейнера. 33 файла,
+3216/−2374.

**Проверено полностью: дерево СОБИРАЕТСЯ и предупреждений не добавило.** Прогон с `--rerun-tasks`
(то есть с настоящей перекомпиляцией Kotlin, `compileFdroidDebugKotlin` отработал, не из кэша) →
`BUILD SUCCESSFUL`. **6 предупреждений против базы в 12 записей, новых ноль**, семь базовых вообще
исчезли. Сообщение коммита `bc89d46` говорит «не проверено» — оно писалось до сборки, верить надо
этому файлу.

Осторожно со счётом предупреждений: нормализация должна идти **после** отбора, а не до. Kotlin пишет
`w: file:///путь/Файл.kt:123:45 текст`, и если сначала срезать путь регуляркой по `^.*/`, вместе с ним
уходит префикс `w:` — последующий `grep '^w: '` не находит ничего и рапортует «ноль предупреждений».
Я на этом один раз попался. Правильно:

```bash
grep -E '^w: |warning:' build.log \
  | sed -E 's#^w: file://[^ ]*/([A-Za-z0-9_]+\.kt)#w: \1#; s/:[0-9]+:[0-9]+/:L:C/g' | sort -u
```

и сверять с `docs/agents/.baseline-warnings.txt`, нормализованным тем же `sed` по `:L:C`.

**Вторая ловушка там же, найдена волной Аккаунта.** Когда Kotlin-демон падает на внутренней ошибке
кодогенерации, Gradle молча переходит на внешний компилятор — и тот печатает предупреждения как
`exception: …: warning:`, а **не** `w:`. Прежний рецепт при таком прогоне выдавал ноль. Оба моих
промаха в подсчёте ошиблись в одну и ту же сторону: в пользу хороших новостей. Поэтому в фильтре
теперь два образца, и **ноль предупреждений — всегда повод посмотреть, не был ли прогон запасным**:

```bash
grep -cE 'Daemon compilation failed|Could not connect to Kotlin compile daemon' build.log
```

Если непусто, счёт по одному `^w: ` бессмыслен.

Где кого остановили:

| Участок | Файлы | Оборвано на |
|---|---|---|
| Главная и навигация | `fragment_home`, `activity_main`, `item_recycler_main`, `MainActivity`, `MainRecyclerAdapter`, drawable строк и навигации | логика пилюли статуса на три состояния в `HomeFragment` — самое вероятное место недописанного |
| Аккаунт | `activity_account`, `AccountFragment`, `SubscriptionPagerAdapter`, устройства, `bg_acc_pill`, `bg_acc_skeleton_ring`, `strings_account` | ожидание сборки |
| Настройки | `fragment_settings_tab` (1681 строка), `SettingsTabFragment`, новый `strings_hub` | **ЗАКОНЧЕНО.** Работа была доведена до конца, просто ни разу не скомпилирована — формулировка «остановлен перед сборкой» вводила в заблуждение. Следующая волна это проверила и доделала три реальных дефекта |
| Подэкраны | список приложений, выбор приложений, маршрутизация, настройки провайдеров, TV, их активности | переписывание настроек провайдеров |
| Начальный экран и прогрузка | — | в самом начале, успел только прочитать задание. Своего кода нет |

## Сборка при нескольких агентах — обязательно изолировать

Ошибка координатора, исправлена по ходу. Всем участкам было велено собираться в одном
`V2rayNG/build`. Четыре параллельных прогона Gradle в одной директории дали **битый APK-zip, падение
Kotlin-демона на `Files.move` и потерю `R.jar`** — 1707 `Unresolved reference 'R'` в 78 файлах разом.
Участок Настроек пересобирал шесть раз, и причина каждый раз была чужая.

Второй промах там же: рецепт предлагал всем писать в общий `build.log` в корне репозитория. Двое
пишут одновременно — счёт предупреждений врёт.

Правильно, у каждого агента своё:

```bash
export ANDROID_HOME=/opt/android-sdk ANDROID_SDK_ROOT=/opt/android-sdk
SP=/tmp/claude-0/-home-user/a3073c71-0c29-5410-a83c-f47c0ca9fa31/scratchpad
cd /home/user/dp/V2rayNG && ./gradlew :app:assembleFdroidDebug --no-daemon \
  --project-cache-dir "$SP/gc-<участок>" > "$SP/build-<участок>.log" 2>&1
```

И узкие задачи вместо полной сборки, когда надо проверить только своё:
`:app:processFdroidDebugResources` — вся разметка и ресурсы, около 20 секунд;
`:app:compileFdroidDebugKotlin` — Kotlin. Падение на `packageFdroidDebug` после того, как эти две
прошли, — чужое, чинить не надо.

**Оговорка, найденная волной Главной: `--project-cache-dir` изолирует не всё.** Каталог вывода
`V2rayNG/build` остаётся общим, и `merged.dir/*.xml` продолжает исчезать посреди чужой задачи при
одновременных прогонах. Своя директория кэша убирает часть гонок, но не эту. Единственное, что
работает надёжно, — **повторить сборку**, и не считать чужое падение своим. Полностью развело бы
только `--project-dir` на отдельные копии дерева или сериализация прогонов.

## Ловушка тем, которую стоит знать всем

Моно-оверлей переопределяет **атрибуты темы**, а не **ресурсы цвета**. `bg_tile_neutral` тянул
`@color/icon_tile_neutral`, и синяя со светлой были правы, а чёрно-белая нет: 25 плиток оставались
синеватым `#20242B` на экране, весь смысл которого в отсутствии цвета. Форма остаётся из drawable,
заливка задаётся `android:backgroundTint="?attr/colorSurfaceContainerHighest"`. `?attr/iconTileBg*`
здесь не подходит — в синей теме он даёт именно цветную плитку.

## В каком порядке продолжать

1. **Честная сборка с `--rerun`** и сверка предупреждений с базой, нормализованно (отбрасывая
   `:строка:столбец`). Это первое, потому что дальше всё поверх.
2. **Настройки** — единственный участок, который ни разу не компилировался. Риск выше всех.
3. **Главная** — дописать пилюлю статуса, довести переезжающую полоску навигации и появление строк
   серверов.
4. **Начальный экран и потоки прогрузки** — не начат вовсе, и это самый объёмный кусок: экранов
   прогрузки в приложении нет, их строить с нуля.
5. **Подэкраны** — тринадцать, лекало §7.
6. **Аккаунт** — доделать.

Запускать агентов заново можно и нужно, но каждому — **непересекающийся набор файлов**. Раскладка,
которая работала: фундамент владеет `values/`+`anim/`+`interpolator/`; Главная — `activity_main` и
`fragment_home` с их адаптерами; Аккаунт — `activity_account` и своё; Подэкраны — четырнадцать своих
разметок плюс `view_toolbar`/`view_row`; Настройки — `fragment_settings_tab`; Начальный —
`layout_home_empty`, оверлей прогрузки, `layout_auth_*`.

## Финальная сверка — ПРОЙДЕНА 2026-08-05

Все восемь пунктов ниже отработаны. Что нашлось и что с этим сделано:

**Системная работоспособность — чисто.** Скриптовая сверка всех `R.id.` и всех цепочек
`binding.…` (включая вложенные `binding.<include>.<child>`) против фактических `@+id` во всех
89 разметках: **ни одной висящей ссылки**. Единственные `R.id.`, которых нет в разметках, —
`android.R.id.content/home`, material'овский `design_bottom_sheet` и `tag_last_click`,
объявленный в `values/ids.xml`.

**Ловушка тем — нашлись четвёртая и пятая, плюс ещё три того же вида.**
`@style/Widget.Departament.Tile` заливался `@color/icon_tile_neutral` и красил глиф
`@color/icon_glyph_neutral` — 22 плитки в девяти разметках; три места (`item_device`,
`view_row`, `RowBinder`) залатали свою плитку `backgroundTint`'ом и оставили причину.
`@color/accent_fill_12` — 12-процентная заливка выбранного под `bg_row`, тремя его краевыми
вариантами, `bg_selectable_item`, `card_bg_selectable` и `bg_server_row`, то есть под выбранной
строкой сервера на Главной. Дальше: разрушающий **текст** вообще не имел атрибута
(`mono_destructiveText` лежал без единого читателя), полоса прогресса `activity_base` звала
`@color/color_fab_active` мимо готового `?attr/indicatorColor`, граница контролов в четырёх
файлах звала `@color/md_theme_outlineControl` мимо готового `?attr/colorOutlineControl`, а
выбранный тариф в «Купить подписку» нёс зашитый `#1F4C8DFF`. Заведены `?attr/colorAccentFill`
и `?attr/colorDestructiveText`. **Проверено скриптом: все 33 цветовых атрибута заданы в
`AppThemeBase` и переопределены в `ThemeOverlay.Mono`, ни одного пропуска.**

**Светлая тема — чисто.** Ни одного `#hex` в атрибутах разметок. Из всех `@color/`,
на которые ссылаются разметки, drawable, `res/color/`, стили и темы, ночного двойника нет
ровно у четырёх, и все четыре намеренные: `btn_gloss_clear`/`_dark` (нулевая альфа),
`colorWhite` (иконка уведомления) и `telegram_brand` (чужая марка, задокументировано в
`colors_onboarding.xml`).

**Ещё три дефекта, не про цвет.** Список серверов рисовал разделитель **дважды** —
`bg_server_row` внутренней тенью по §4 и вдобавок старый `DividerItemDecoration`, который вёл
свою линию и под выбранной строкой, сквозь низ акцентной рамки (ровно грабля №3). «+» на
Главной и «✕» на баннере отвечали на нажатие **ничем**: оба звали
`selectableItemBackgroundBorderless`, а ripple выключен продуктом. Выбор срока в «Купить
подписку» менял себе радиус с 14 на 20 — грабля №2, под комментарием, обещающим обратное.

**Тайминги — сверены покадрово и по исходнику прототипа.** Таблица сборки §3 совпадает до
числа (460/60 · 460/130 · 720/180 · 520/420 · 520/500 · 560/580 · 560/700+85), кадр 6700 мс
прототипа показывает ровно то же состояние, что даёт `entranceSteps()`. Уход оверлея: пауза
после последнего шага была 1400 мс против прототиповых 1300 (`runFlow`: 4600 → 5900) —
исправлено; таяние 520 мс `ease_screen` и `scale(1.06)` совпадали.

**Производительность — чисто.** Три аппаратных слоя, все три снимаются в `doOnEnd`. Сборка —
один `AnimatorSet` со стартовыми задержками. Строки серверов — `doOnPreDraw`, только
прикреплённые дети, сброс в `onViewRecycled`. Оверлей удаляется из иерархии той же строкой,
что запускает сборку. Буфер читается только на переднем плане и по нажатию.

**Осознанное отклонение, одно: оборот дуги на кнопке подключения.** Подробности и named route —
в комментарии `fragment_home.xml` над `connect_sweep`.

Мандат ниже — то, чем волна была. Оставлен как есть.

> «поставь агента который потом пройдется по коду и все сверит по дизайну и тд, если будут ошибки
> чтобы чинилось»

Запускается **после того как лягут все шесть участков**, не раньше: раньше ему нечего сверять.
Это не отчёт, а волна работы — он **чинит найденное**, а не составляет список претензий.

Что он делает:

1. Открывает прототип и проходит **каждый** экран и состояние: обе темы, оба состояния трафика, все
   окошки выбора, все тринадцать подэкранов, оба потока прогрузки, цикл подключения. Снимает
   скриншоты.
2. Сверяет с реализацией **по числам**, а не на глаз: размеры, отступы, скругления, цвета, тайпшкала.
   Каждое расхождение — либо чинит, либо, если чинить нельзя, объясняет почему.
3. Проверяет тайминги **покадрово**, а не по коду: кликает и снимает по меткам времени. Особенно
   таблицу сборки главной §3 и выход оверлея прогрузки.
4. Проверяет, что **ничего не пропало**: проходит по 32 пунктам Части 2 в `PORT-DELTA.md` и
   подтверждает каждый по факту в коде, а не по памяти.
5. Проверяет **системную работоспособность**: что `@+id`, к которым обращается Kotlin, на месте —
   пинг, вход через Telegram, обновление подписки, сохранение переключателей. Это то, что ломается
   тихо и чего не видит ни линтер, ни сборка.
6. Проверяет **светлую тему** на каждом экране — она обязана работать, а её легко потерять, потому
   что владелец её не смотрит.
7. Проверяет производительность: аппаратные слои сняты после анимаций, кольцо не крутится в фоне,
   анимация строк серверов разовая и только для видимых, оверлей удалён из иерархии, нет
   `postDelayed`-цепочек.
8. Собирает и сверяет предупреждения.

Чего он **не** делает: не переделывает то, что уже соответствует дизайну, и не расширяет объём.
Правило владельца в силе — интерфейс и формулировки по дизайну, возможности по репозиторию.

## Стык, который нельзя потерять

Снятие оверлея прогрузки и запуск сборки главной обязаны идти **в одной транзакции** — иначе главная
мелькнёт целиком до начала анимации (грабля №6). Оверлеем владеет участок начального экрана, таблицей
появления (§3: строка аккаунта 460/60, «+» 460/130, кольцо 720/180, скорости 520/420, имя сервера
520/500, карточка 560/580, строки серверов 560/700 + 85 мс на каждую) — участок главной. Контракт
между ними не написан, его надо задать явно.

## Решения владельца — приняты, не пересматривать

- **Радиус кнопок 24dp**, капсульная форма. Прежний отказ от капсульных CTA отменён. Уже в
  `radius_button`.
- **Пилюля статуса на главной — все три состояния**, включая «Не подключено». Прежнее указание убрать
  это слово отменено.
- **Интерфейс и формулировки — по дизайну, возможности — по репозиторию.** Тексты берутся из
  прототипа дословно. Правило «ничего не удалять» касается фишек, а не формулировок.
- **Режим — по тексту дизайна**: «TUN» и «Только прокси». Третье значение в список выбора не
  добавляется, но код, который его умеет, остаётся, и уже сохранённая настройка ломаться не должна.
- **Вход по e-mail, OTP, 2FA, Google в интерфейс не выносится** — сворачивается в кнопку «Войти через
  сайт» под «Другими способами» на начальном экране. Код авторизации остаётся в проекте.
- **Space Grotesk не набирает кириллицу** — у него ноль кириллических кодпоинтов, в прототипе это
  «работает» только потому, что браузер молча подставляет системный шрифт. Русские заголовки набирать
  шрифтом, который кириллицу содержит; Space Grotesk оставить брендовым для латиницы.

## Правила, которые не отменяются ничем

- **`@+id` связаны с логикой.** Переименование тихо ломает пинг, вход через Telegram, сохранение
  настроек — и ни один линтер этого не заметит. Перед изменением искать все ссылки в Kotlin.
  Сохранить существующий id лучше, чем придумать красивый.
- **Светлая тема должна работать.** Разметка одна на все темы, цвета через `?attr/...`, плитки через
  `?attr/iconTileBg*` / `?attr/iconTint*`.
- **Ничего существующего не удалять.** 32 пункта в Части 2 дельты.
- **Без лишней нагрузки.** Аппаратный слой снимать после анимации; кольцо не пульсирует постоянно;
  дуга — одна бесконечная анимация, а не перезапуск по таймеру; анимация появления строк серверов
  разовая и только для видимых, иначе тридцатая строка получит задержку в три секунды; оверлей после
  снятия удалять из иерархии, а не прятать; никаких `postDelayed`-цепочек там, где хватает
  `AnimatorSet`.
- **Буфер обмена читается один раз** при выходе на передний план и по нажатию, никогда по таймеру.
  Экран с найденной ссылкой показывается только когда подписок нет, то есть примерно раз за жизнь
  установки — защиту от несуществующей проблемы не городить, карточка должна появляться сама.

## Проверки, которые делать самостоятельно

Сборка после каждого участка. Скриншоты прототипа для сверки внешнего вида. Тайминги — покадрово:
кликнуть и снимать по меткам времени, пример `flow.mjs` в скретчпаде, рецепт в
`КАК-ОТКРЫТЬ-ПРОТОТИП.md`.

**APK здесь запустить нельзя** — нативной библиотеки нет. Всё визуальное судится статически. Первый
настоящий взгляд будет у владельца, на CI-сборке.

## Напоминания

- Ежечасно, `trig_01LA94Y1fnwkeRsYj7nwp5QL` — проверка PR **s-erlish/dp#3** (черновик, открыт).
  Молчать, если ничего не изменилось.
- Одноразово в 23:01 UTC — начало полной автономной работы.

PR #3 нацелен в `claude/app-audit-agents-hyyftk`, а не в `master`. Это устаревшая база; сменить её
стоит, но это решение владельца, а не побочный эффект переноса.
