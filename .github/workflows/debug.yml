name: Build Debug APK

on:
  workflow_dispatch:
  push:
    branches:
      - claude/vpn-client-happ-design-mq51pv
    paths:
      - 'V2rayNG/**'
      - '.github/workflows/debug.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: 'recursive'
          fetch-depth: '0'

      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Fetch AndroidLibXrayLite tag
        run: |
          pushd AndroidLibXrayLite
          CURRENT_TAG=$(git describe --tags --abbrev=0)
          echo "CURRENT_TAG=$CURRENT_TAG" >> $GITHUB_ENV
          popd

      - name: Download libv2ray.aar
        uses: robinraju/release-downloader@v1.12
        with:
          repository: '2dust/AndroidLibXrayLite'
          tag: ${{ env.CURRENT_TAG }}
          fileName: 'libv2ray.aar'
          out-file-path: V2rayNG/app/libs/

      - name: Grant gradlew permission
        working-directory: V2rayNG
        run: chmod +x ./gradlew

      - name: Build debug APK (arm64-v8a, playstore only)
        working-directory: V2rayNG
        run: ./gradlew assemblePlaystoreDebug -PABI_FILTERS=arm64-v8a

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: departament-debug-apk
          path: V2rayNG/app/build/outputs/apk/**/*arm64-v8a*.apk
