<?xml version="1.0" encoding="utf-8"?>
<!--
  Segmented-control item fill. Unselected is transparent so the track reads as one
  object; selected is colorPrimaryContainer #17325C. Selection carries three axes —
  fill, colour and weight 700 (see @style/TextAppearance.Nav.Segment.Active) — so it
  survives the mono theme and colour-blindness. Never tint alone (00-rules.md 7.1).
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:alpha="0.38"
        android:color="?attr/colorPrimaryContainer"
        android:state_checked="true"
        android:state_enabled="false" />
    <item android:color="?attr/colorPrimaryContainer" android:state_checked="true" />
    <item android:color="@android:color/transparent" />
</selector>
