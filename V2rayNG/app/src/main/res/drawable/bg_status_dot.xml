<?xml version="1.0" encoding="utf-8"?>
<!-- Status dot: plain oval tinted in code (connected / idle / error).
     Defaults to the accent blue; never hardcoded white. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="?attr/colorPrimary" />
</shape>
