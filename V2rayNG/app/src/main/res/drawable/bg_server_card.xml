<?xml version="1.0" encoding="utf-8"?>
<!-- Tappable Incy server card: rounded ripple over a dark surface with a 1dp top rim-light. -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorPrimaryContainer">
    <item>
        <layer-list>
            <!-- Base card surface -->
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="?attr/colorSurface" />
                    <corners android:radius="@dimen/radius_card" />
                    <stroke
                        android:width="1dp"
                        android:color="?attr/colorOutlineVariant" />
                </shape>
            </item>
            <!-- Rim-light: 1dp top edge, inset 2dp from the sides -->
            <item
                android:left="2dp"
                android:right="2dp"
                android:gravity="top"
                android:height="1dp">
                <shape android:shape="rectangle">
                    <solid android:color="@color/card_edge_highlight" />
                </shape>
            </item>
        </layer-list>
    </item>
    <!-- Round the ripple to the card radius -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />
            <corners android:radius="@dimen/radius_card" />
        </shape>
    </item>
</ripple>
