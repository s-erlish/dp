<?xml version="1.0" encoding="utf-8"?>
<!-- Incy card: dark rounded surface + hairline outline. No top rim-light here: on the
     compact top-bar surfaces this sits on (account chip / link-Telegram CTA) the 1dp
     highlight read as a stray border, so the edge is a single even outline on all sides. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="?attr/colorSurface" />
    <corners android:radius="@dimen/radius_card" />
    <stroke
        android:width="1dp"
        android:color="?attr/colorOutlineVariant" />
</shape>
